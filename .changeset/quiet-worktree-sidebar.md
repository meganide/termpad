---
'termpad': patch
---

Fix duplicate worktree entries in the sidebar when worktree creation overlaps with automatic discovery. Reuse the existing session so terminals stay attached to the correct worktree.
