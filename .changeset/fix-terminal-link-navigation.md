---
'termpad': patch
---

Fix terminal hyperlinks opening in the default browser without a warning popup, and prevent right-clicks and macOS Ctrl-clicks from opening links so the copy menu remains usable.
